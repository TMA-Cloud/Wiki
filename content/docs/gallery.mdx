---
title: "UI Gallery"
description: "A visual tour of TMA Cloud — every screen you'll see as a user, admin, or API integrator."
---

import { GalleryCard } from "@/components/GalleryCard";

<div className="gallery-section">
  <span className="gallery-section__label">Authentication</span>
  <h2>Sign in & create an account</h2>
  <div className="gallery-section__desc">
    Email + password, with optional Google OAuth and MFA. The first signup on a fresh deployment becomes the admin.
  </div>
  <div className="gallery-grid gallery-grid--pair">
    <GalleryCard img="login.png" title="Login" subtitle="Email, password, and MFA prompt when enabled" />
    <GalleryCard img="signup.png" title="Sign up" subtitle="Account creation — only shown when signup is enabled" />
  </div>
</div>

<div className="gallery-section">
  <span className="gallery-section__label">Dashboard</span>
  <h2>Your home screen</h2>
  <div className="gallery-section__desc">
    Quick access to recent files, storage usage, and shared items. Fully responsive.
  </div>
  <div className="gallery-grid">
    <GalleryCard img="dashboard.png" tag="Desktop" title="Dashboard" subtitle="Sidebar navigation, recents, and storage summary" />
    <GalleryCard img="dashboard(mobile).png" tag="Mobile" portrait title="Dashboard" subtitle="Collapsed navigation, touch-friendly controls" />
  </div>
</div>

<div className="gallery-section">
  <span className="gallery-section__label">File manager</span>
  <h2>Browse, organize, and upload</h2>
  <div className="gallery-section__desc">
    Drag-and-drop uploads, bulk actions, breadcrumb navigation, and a sortable list view — works the same across devices.
  </div>
  <div className="gallery-grid">
    <GalleryCard img="file-manager.png" tag="Desktop" title="File Manager" subtitle="Full list view with sort, multi-select, and bulk actions" />
    <GalleryCard img="file-manager(mobile).png" tag="Mobile" portrait title="File Manager" subtitle="Compact rows with swipe-friendly action menus" />
  </div>
</div>

<div className="gallery-section">
  <span className="gallery-section__label">Previews & editing</span>
  <h2>View and edit in the browser</h2>
  <div className="gallery-section__desc">
    Images preview inline; Office documents open in OnlyOffice when the integration is configured.
  </div>
  <div className="gallery-grid gallery-grid--pair">
    <GalleryCard img="image-viewer.png" title="Image Viewer" subtitle="Full-screen preview with zoom and next/prev" />
    <GalleryCard img="document-viewer.png" title="Document Viewer" subtitle="OnlyOffice integration for .docx, .xlsx, .pptx, .pdf" />
  </div>
</div>

<div className="gallery-section">
  <span className="gallery-section__label">Sharing</span>
  <h2>Share files and folders</h2>
  <div className="gallery-section__desc">
    Create public share links with optional expiry, copy them, or revoke access any time.
  </div>
  <div className="gallery-grid gallery-grid--pair">
    <GalleryCard img="share-files.png" title="Share dialog" subtitle="Choose expiry, copy link, or stop sharing" />
    <GalleryCard img="share-link.png" title="Public share page" subtitle="What anonymous recipients see when they open the link" />
  </div>
</div>

<div className="gallery-section">
  <span className="gallery-section__label">Upload & starred</span>
  <h2>Getting files in — and finding them fast</h2>
  <div className="gallery-grid gallery-grid--pair">
    <GalleryCard img="upload-modal.png" title="Upload modal" subtitle="Drag-and-drop, per-file progress, cancel in-flight" />
    <GalleryCard img="starred-files.png" title="Starred files" subtitle="Quick access to the files you've pinned" />
  </div>
</div>

<div className="gallery-section">
  <span className="gallery-section__label">Settings</span>
  <h2>User & admin controls</h2>
  <div className="gallery-section__desc">
    Every user gets Security and Session controls. The admin (first signup) sees additional sections for signup, users, OnlyOffice, custom domain, and updates.
  </div>
  <div className="gallery-grid">
    <GalleryCard img="settings.png" title="Settings overview" subtitle="Top-level navigation for account and app settings" />
    <GalleryCard img="security-section.png" title="Security" subtitle="Password change and MFA controls (per user)" />
    <GalleryCard img="active-sessions.png" title="Active sessions" subtitle="See every device, revoke any one, or sign out everywhere" />
    <GalleryCard img="admin-section.png" title="Admin section" subtitle="Signup, extensions, password change, desktop-only mode" />
    <GalleryCard img="all-users.png" title="Registered users" subtitle="Admin view — storage usage per user and quota overrides" />
    <GalleryCard img="only-office-section.png" title="OnlyOffice" subtitle="Connect the document server and set the JWT secret" />
    <GalleryCard img="custom-share-url.png" title="Custom share domain" subtitle="Serve share links on a dedicated domain" />
    <GalleryCard img="update-section.png" title="Updates" subtitle="Version info and update controls" />
  </div>
</div>

---

Jump to the [Introduction](/docs) or start the [Getting Started guide](/docs/getting-started/overview).
